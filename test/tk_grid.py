from tkinter import*

root = Tk()

#text 
mylabel1 = Label(root, text="hello").grid(row=0, column=0)
mylabel2 = Label(root, text="hello").grid(row=1, column=0)
mylabel3 = Label(root, text="hello").grid(row=2, column=0)
#put into window
# elment is fix
# mylabel.grid()

root.mainloop()